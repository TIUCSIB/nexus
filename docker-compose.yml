version: '3.8'
services:
  nexus:
    build: .
    ports:
      - "8080:8080"
      - "9090:9090"
    volumes:
      - nexus-data:/app/data
      - ./config.yaml:/app/config.yaml
    restart: unless-stopped

volumes:
  nexus-data: